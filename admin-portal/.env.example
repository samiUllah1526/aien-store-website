# Backend API base URL (no trailing slash)
PUBLIC_API_URL=http://localhost:3000

# For create/edit/delete and image upload, set a JWT in the browser:
# localStorage.setItem('admin_token', 'YOUR_JWT')
