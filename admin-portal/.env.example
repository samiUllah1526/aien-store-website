# Admin Portal Configuration
# Copy to .env and fill in values. See site.config.example for full documentation.

# Backend API URL (no trailing slash). Required for all admin operations.
PUBLIC_API_URL=http://localhost:3000

# Admin app name (default: E-Commerce Admin)
# PUBLIC_ADMIN_APP_NAME=E-Commerce Admin

# Favicon path (default: /favicon.svg)
# PUBLIC_ADMIN_FAVICON=/favicon.svg

# Storage keys (optional; change if you need to avoid conflicts)
# PUBLIC_ADMIN_THEME_STORAGE_KEY=admin_dark
# PUBLIC_ADMIN_AUTH_TOKEN_KEY=admin_token
# For manual JWT injection: localStorage.setItem('<auth key>', 'YOUR_JWT')

# Login redirect path (default: /admin/login)
# PUBLIC_ADMIN_LOGIN_PATH=/admin/login

# Main storefront URL (optional)
# PUBLIC_MAIN_WEBSITE_URL=https://yoursite.com
