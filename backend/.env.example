# Configuration is loaded from env vars (see src/config/). To use a secrets manager later,
# add an async loader that returns overrides — no app code changes needed.
#
# Server
PORT=3000
NODE_ENV=development
# CORS allowed origins (comma-separated). Leave unset for dev defaults (localhost:4321, etc.)
# CORS_ORIGIN=https://admin.example.com,https://www.example.com

# Database (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/adab?schema=public"
# Optional: separate DB for integration tests (defaults to DATABASE_URL)
# DATABASE_URL_TEST="postgresql://USER:PASSWORD@localhost:5432/adab_test?schema=public"

# JWT (auth)
JWT_SECRET="change-me-in-production"
# Access token lifetime in seconds (default 86400 = 24h). Admin sessions use this.
# JWT_ACCESS_EXPIRES_SEC=86400

# Public URLs (used in emails and reset links). Set both; do not leave as example.com in production.
# Main website (storefront) base URL — where customers shop and use forgot-password.
# APP_URL=http://localhost:4321
# Admin portal base URL — where staff sign in and use forgot-password.
# ADMIN_URL=http://localhost:4322
# Optional: override admin login URL (default is ADMIN_URL + /admin/login).
# ADMIN_LOGIN_URL=http://localhost:4322/admin/login

# Mail (provider-agnostic: set MAIL_PROVIDER and the matching API key)
# MAIL_PROVIDER=brevo
# MAIL_FROM_EMAIL=orders@yourdomain.com
# MAIL_FROM_NAME=Your Store
# Brevo (when MAIL_PROVIDER=brevo). Get key: https://app.brevo.com/settings/keys/api
# BREVO_API_KEY=
# SendGrid (when MAIL_PROVIDER=sendgrid)
# SENDGRID_API_KEY=

# Seed: first admin user. Run after DB is ready: npx prisma db push (or migrate), then npm run db:seed
# SEED_ADMIN_EMAIL=admin@example.com
# SEED_ADMIN_PASSWORD=Admin123!
# SEED_ADMIN_NAME=Admin

# Storage provider: 'cloudinary' | 's3' | 'local'. Default: cloudinary when CLOUDINARY_* set.
# STORAGE_PROVIDER=cloudinary

# Cloudinary (when STORAGE_PROVIDER=cloudinary). Get credentials: https://console.cloudinary.com/settings/api-keys
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
