/**
 * Order-related constants. Single source of truth for server-side calculations.
 * Used by quote and order creation so totals are never derived from client.
 */
export const SHIPPING_COST_CENTS = 299;
