---
/**
 * Order detail by ID. Rendered on demand (SSR) so any order URL works without build-time paths.
 */
import BaseLayout from '../../../layouts/BaseLayout.astro';
import OrderDetailPage from '../../../components/account/OrderDetailPage';

export const prerender = false;

const { id } = Astro.params;
---

<BaseLayout title="Order — Adab" description="Order details.">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 py-10 md:py-14">
    <OrderDetailPage client:load orderId={id ?? ''} />
  </div>
</BaseLayout>
