---
import BaseLayout from '../../layouts/BaseLayout.astro';
import FavoritesPage from '../../components/account/FavoritesPage';
import { brandName, favoritesMetaDescription } from '../../config';
---

<BaseLayout title={`Favorites — ${brandName}`} description={favoritesMetaDescription}>
  <div class="max-w-6xl mx-auto px-4 sm:px-6 py-10 md:py-14">
    <h1 class="font-display text-3xl text-ink dark:text-cream mb-8">Favorites</h1>
    <FavoritesPage client:load />
  </div>
</BaseLayout>
