---
/**
 * Product detail only: `/shop/:productSlug`.
 * Category listings live under `/shop/category/:categorySlug`.
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProductDetailSection from '../../components/product/ProductDetailSection';
import AnimatedUrduVerse from '../../components/AnimatedUrduVerse';
import { formatMoney } from '../../lib/formatMoney';
import { storeApiBaseUrl, brandName, siteUrl } from '../../config';

export async function getStaticPaths() {
  const limit = 100;
  const slugs: string[] = [];
  try {
    let page = 1;
    let hasMore = true;
    while (hasMore) {
      const url = `${storeApiBaseUrl}/products?page=${page}&limit=${limit}&sortBy=slug&sortOrder=asc`;
      const res = await fetch(url);
      const json = await res.json();
      if (!res.ok || !json.success || !Array.isArray(json.data)) break;
      const pageSlugs = json.data.map((p: { slug: string }) => p.slug).filter(Boolean);
      slugs.push(...pageSlugs);
      const total = json.meta?.total ?? 0;
      hasMore = slugs.length < total && pageSlugs.length === limit;
      page += 1;
    }
  } catch (err) {
    console.error('[getStaticPaths] products error:', err);
  }

  return slugs.map((slug: string) => ({ params: { slug } }));
}

const slug = Astro.params.slug;
if (!slug) {
  return Astro.redirect('/shop');
}

function toFullImageUrl(url: string): string {
  if (!url) return '';
  return url.startsWith('http') ? url : `${storeApiBaseUrl}${url.startsWith('/') ? '' : '/'}${url}`;
}

let product: {
  id: string;
  name: string;
  slug: string;
  description: string | null;
  price: number;
  currency: string;
  image: string;
  images: string[];
  variants: Array<{
    id: string;
    color: string;
    size: string;
    stockQuantity: number;
    priceOverrideCents?: number | null;
    isActive: boolean;
    image?: string;
    images?: string[];
  }>;
  sizes?: string[];
  inStock: boolean;
  urduVerse?: string | null;
  urduVerseTransliteration?: string | null;
  categories?: Array<{ name: string; slug: string }>;
} | null = null;

try {
  const productUrl = `${storeApiBaseUrl}/products/slug/${encodeURIComponent(slug)}`;
  const res = await fetch(productUrl);
  const json = await res.json();
  if (res.ok && json.success && json.data) {
    const p = json.data;
    const image = toFullImageUrl(p.image ?? '');
    const images = Array.isArray(p.images) ? p.images.map((img: string) => toFullImageUrl(img)).filter(Boolean) : (image ? [image] : []);
    const variants = (Array.isArray(p.variants) ? p.variants : []).map((variant: { image?: string; images?: string[] } & Record<string, unknown>) => {
      const variantImage = toFullImageUrl((variant.image as string) ?? '');
      const variantImages = Array.isArray(variant.images)
        ? variant.images.map((img: string) => toFullImageUrl(img)).filter(Boolean)
        : (variantImage ? [variantImage] : []);
      return {
        ...variant,
        image: variantImage,
        images: variantImages,
      };
    });
    product = {
      id: p.id,
      name: p.name,
      slug: p.slug,
      description: p.description ?? null,
      price: p.price,
      currency: p.currency,
      image,
      images: images.length > 0 ? images : (image ? [image] : []),
      variants,
      sizes: p.sizes,
      inStock: p.inStock !== false,
      urduVerse: p.urduVerse ?? null,
      urduVerseTransliteration: p.urduVerseTransliteration ?? null,
      categories: p.categories ?? [],
    };
  }
} catch (err) {
  console.error('[product page] fetch error:', err);
}

if (!product) {
  return Astro.redirect('/shop');
}

const defaultVariant =
  product.variants.find((variant) => variant.isActive && variant.stockQuantity > 0) ??
  product.variants.find((variant) => variant.isActive) ??
  null;
const defaultVariantImages = defaultVariant?.images?.length
  ? defaultVariant.images
  : defaultVariant?.image
    ? [defaultVariant.image]
    : [];
const displayImages =
  defaultVariantImages.length > 0
    ? [...defaultVariantImages, ...product.images.filter((img) => !defaultVariantImages.includes(img))]
    : product.images;

const canonicalUrl = siteUrl ? `${siteUrl}/shop/${product.slug}` : undefined;
const ogImage = displayImages?.[0] ?? product.image;
const ogImageAbs = ogImage && (ogImage.startsWith('http') ? ogImage : `${siteUrl || storeApiBaseUrl}${ogImage.startsWith('/') ? '' : '/'}${ogImage}`);

const productImages = (displayImages ?? (product.image ? [product.image] : [])).map((img: string) =>
  img.startsWith('http') ? img : `${siteUrl || storeApiBaseUrl}${img.startsWith('/') ? '' : '/'}${img}`,
);
const productJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Product',
  name: product.name,
  description: product.description ?? undefined,
  image: productImages.length ? productImages : undefined,
  offers: {
    '@type': 'Offer',
    price: product.price / 100,
    priceCurrency: product.currency,
    availability: product.inStock ? 'https://schema.org/InStock' : 'https://schema.org/OutOfStock',
  },
  ...(canonicalUrl && { url: canonicalUrl }),
};
---

<BaseLayout
  title={`${product.name} — ${brandName}`}
  description={product.description ?? undefined}
  image={ogImageAbs}
  canonical={canonicalUrl}
  jsonLd={productJsonLd}
>
  <article class="max-w-5xl mx-auto px-4 sm:px-6 py-16 md:py-24">
    <ProductDetailSection
      client:load
      productId={product.id}
      name={product.name}
      slug={product.slug}
      price={product.price}
      currency={product.currency}
      image={product.image}
      images={product.images}
      variants={product.variants}
      inStock={product.inStock}
    >
      {product.urduVerse && (
        <AnimatedUrduVerse
          client:load
          verse={product.urduVerse}
          transliteration={product.urduVerseTransliteration}
        />
      )}

      <h1 class="font-display text-2xl md:text-3xl text-soft-charcoal dark:text-off-white">
        {product.name}
      </h1>

      {product.description && (
        <p class="mt-6 text-soft-charcoal/90 dark:text-off-white/90 leading-relaxed max-w-prose">
          {product.description}
        </p>
      )}

      <p class="mt-8 text-ash text-sm">
        {formatMoney(product.price, product.currency)}
      </p>

      {product.categories?.length ? (
        <p class="mt-1 text-ash text-sm">
          {product.categories.map((c) => c.name).join(', ')}
        </p>
      ) : null}
    </ProductDetailSection>
  </article>
</BaseLayout>
